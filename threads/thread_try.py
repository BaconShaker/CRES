#!/bin/usr/python
# This is my attempt at setting up a class\
# then use the module to call if  __name__ == "main" do something\
# and see if this fixes the problem in the root.mainloop() in the ttk GUI.
